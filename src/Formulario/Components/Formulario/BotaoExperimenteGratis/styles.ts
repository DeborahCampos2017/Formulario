import { blue } from "@mui/material/colors";

const useStyles = () => ({
  container: {
    backgroundColor: blue[800],
    padding: 3,
    borderRadius: 3,
  },
  titulo: {
    fontWeight: 'bold',
    color: '#ffffff',
  },
  text: {
    color: '#ffffff',
  }
});

export default useStyles;
